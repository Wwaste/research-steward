# Research Steward v0.2 → v1.0 交接文档

生成：2026-09-05（Claude，主实施者）。上一份背景：`docs/HANDOFF_TO_CLAUDE_2026-08-31.md`（Codex 监督记录，§18–27）。

## 0. 一页现状

- 31 个 Task / 155 个 checkbox 中：**8 个 Task 已触碰（40 格：21 done / 18 partial / 1 missing），23 个 Task 未开始（115 格）**。按格加权完成度约 **19–20%**；因 Codex 对 1.5/2.1/2.4/2.5 出具 changes-requested，有效完成度应更保守。
- Codex 监督 finding **RS-V1-SUP-001…020**：Critical 013（eval fixture PII+虚构人工裁决）**已修**（含未发布历史改写）；带 RED 反例的 014/016/017/018/019 未修，为下一优先；Slice A（Phase 1）gate **NOT PASSED**。
- 分支 `claude/research-steward-v02-v1` 全量：24 test files / 294 tests 全绿（高并发下 workflow-concurrency/package-handoff 偶发超时 flake，隔离重跑稳定通过——测试稳定性在 backlog），typecheck、coverage(82+/82+/90+/82+ vs 阈值 75/77/84/75)、audit 0 vuln（Codex 于 82bd9f3 独立复跑确认过 167-test 时点）。

## 1. 项目与环境

| 项 | 值 |
|---|---|
| 源仓库 | `/Users/waste/research-steward`（GitHub `Wwaste/research-steward`；main=`18aefe8`；tag `v0.1.0`=`b144b3b` 不可动） |
| 实施 worktree | `/Users/waste/research-steward-worktrees/v02-v1`，分支 `claude/research-steward-v02-v1`，merge-base `18aefe8` |
| 插件目录 | `plugins/research-steward/`（**测试必须从这里跑**：`npx vitest run`；从仓库根跑会因 cwd 依赖出现 3–4 个假失败） |
| 唯一计划 | `docs/superpowers/plans/2026-08-31-research-steward-v0.2-to-v1-roadmap.md`，SHA-256 `916da1ee37cfffbf3587bcdcdcd66b15579b37b4f6a8f37b4d464a29d32c65ca`（749 行；旧 39b465… 版本无效） |
| 执行 prompt | `docs/superpowers/plans/CLAUDE_EXECUTE_RESEARCH_STEWARD_V02_TO_V1_PROMPT.md`，SHA-256 `19519eec184730f3dbd055204eb3348ddca2cf52424192ed1d34a1aa83e57898` |
| 持久状态（gitignored，本地） | `/Users/waste/research-steward/Made by Claude Code/`：`execution-ledger.md`（首行 PLAN 路径；一切裁决在此）、`IMPLEMENTATION_PROGRESS.md`、`CODEX_REVIEW_QUEUE.md`、`CROSS_TASK_CONFLICT_SCAN.md`、`implementation-milestones/TASK-*.md` |
| Codex 监督通道 | `docs/HANDOFF_TO_CLAUDE_2026-08-31.md`（gitignored；Codex 只读仓库+写此文件；**Codex 不得修改 Made by Claude Code/**） |
| 恢复协议 | 新会话先读 ledger 首行与本文档；以 Git log/里程碑恢复，不凭记忆重跑；用户级审批集中写 `Made by Claude Code/USER_ACTION_REQUIRED.md`（当前无待批项） |

安全门槛（未解除）：不 push/merge/tag/release 到 main、不动 v0.1.0、不改真实 VPS/Cloudflare/HPC/第三方、不用单独计费 API（DeepSeek 等）、秘密不入代码/日志/事件。本分支的推送为用户 2026-09-05 明确指令。

## 2. 提交链（18aefe8..HEAD）与历史改写

```
9ccb360 docs(plan): add authorized v0.2-to-v1 roadmap and execution prompt baseline
079dbb0 Task 1.1: pin stable install to v0.1.0, add coverage gate, split CI, installed-plugin smoke
94a2214 Task 1.2: add capability inventory module (research doctor core)
425835a Task 1.3: add seven research presets, plan builder, and workflow lock module
4989f75 Task 1.4: add dry-run forecast module
e72c640 Tasks 1.2-1.4 wiring: doctor/build-plan/dry-run across CLI, MCP, and schemas
82bd9f3 Wiring follow-up: installed smoke replica carries all six public schemas
0023ae0 Task 1.5: add acceptance preparation helper and attention digest modules
6b4cb89 Task 2.1: typed provider failure classes and conservative retry policy
333292b Task 2.5: local telemetry spans and zero-cost evaluation scaffolding
7165255 Task 2.4: rebuildable ledger index cache and offline maintenance toolkit
e4af416 Task 2.5 fix: enforce eval fixture provenance hygiene (RS-V1-SUP-013)
```

2026-09-05 对未发布历史做过一次定点改写（Critical RS-V1-SUP-013：原 `fed8916` 把用户真实姓名+私人邮箱写进 eval fixture）：`fed8916→333292b`（仅清洗 provenance，非 provenance 字段 JSON 语义等价）、`c6878df→7165255`（原样重放）、新增 `e4af416`（机器强制 provenance 卫生）。改写前完整链保留在**仅本地**分支 `backup/pre-pii-rewrite-c6878df`。推送后请勿再改写本分支历史。

推送记录：首次推送被 GitHub push protection 拦截——telemetry 脱敏测试里的假 Slack token 样例（xoxe-1-…）被误判为真凭据。裁决：不走解封链接（避免在公开历史中留下逼真 token 形状），把样例改为无数字段的 `xoxe-fake-redaction-probe`（仍命中自家脱敏正则，测试有效性不变），经用户放行后对未发布历史做了第二次定点重放（备份分支 `backup/pre-push-fix-ac7bf51`，仅本地）。最终链即上方提交列表，2026-09-05 已推送：`origin/claude/research-steward-v02-v1 = d55bb28`。

## 3. 155 格 checkbox 全量状态

图例：✅ done ｜ 🟡 partial ｜ ❌ missing ｜ ⬜ 未开始。已触碰 8 任务的逐格状态来自 2026-08-31 的 9-agent 逐格审计（对照磁盘+实测），审计后唯一状态变化是 013 修复（不翻转任何格）。

### Task 1.1: 修复 v0.1 文档、分发和 coverage 摩擦  — done 3 / partial 2 / missing 0

- ✅ 先写失败测试，断言稳定安装示例固定 `v0.1.0` 或最新 stable tag，状态文本不再称 release candidate。
- ✅ 加入与 Vitest 版本兼容的 coverage provider，并设置语句/分支/函数/行的初始基线；阈值只允许随版本上升。
- 🟡 写“独立临时 CODEX_HOME + tag marketplace + 全新 client session”分发 smoke，验证 8 个 skills、13 个 MCP tools 和 client roots 首调用。
  - 缺口：scripts/smoke-installed-plugin.mjs 存在且实测通过：replica 模式验证 8 skills、16 tools（计划写 13，后续 task 新增工具所致）、client roots 首调用（删除 RESEARCH_STEWARD_ROOTS 后经 ListRoots 授权）；缺：计划点名的 tag marketplace 路径没有任何模式执行——默认模式从 checkout 复制 replica，手动 --use-codex-cli 模式虽用临时 CODEX_HOME 但 marketplace add 的是本地 checkout 而非 v0.1.0 ta…
- ✅ 在 CI 中把源码测试、bundle identity、安装缓存 smoke 分开呈现，避免把 dev dependency 缺失误报为发行失败。
- 🟡 运行 `npm ci && npm run check && npm run test:coverage`，随后从 tag 安装缓存运行独立 smoke。
  - 缺口：本次实测 npm run typecheck 通过；npm run test:coverage 全量 290/290 通过且覆盖率达标（首次全量出现 6 个偶发失败：workflow-concurrency/package-handoff 超时与临时目录 ENOTEMPTY，隔离重跑 13/13 通过，属后续 task 的测试文件）；smoke-installed-plugin.mjs 通过且 dist/schemas 无 git 脏改动；缺："从 tag 安装缓存运行独立 smoke"未实现，smoke 复制的是当前 checkout 而非 v0.1.0 tag 的安装缓存（与第 3 条同一…
  - 审计备注：审计对象：/Users/waste/research-steward-worktrees/v02-v1/plugins/research-steward（计划文件 /Users/waste/research-steward/docs/superpowers/plans/2026-08-31-research-steward-v0.2-to-v1-roadmap.md 第 220 行起）。仓库有 v0.1.0 tag。核心交付物（测试、coverage 基线、smoke 脚本、CI 拆分）都已落地并有测试证据；唯一系统性缺口是"从 tag 安装"这条路径始终未被真正执行，影响第 3、5 两条。另注意全量 suite 在并发压力下有偶发 flake（不在 1.1 范围内），单跑稳定。

### Task 1.2: 增加能力清单与 `research doctor`  — done 2 / partial 3 / missing 0

- ✅ 定义 `DoctorCheck`/`DoctorReport`，每项只返回 `pass | warn | fail | skipped`、证据摘要和修复建议，不输出 token 或秘密。
- 🟡 检查 Node、bundle、schema、plugin discovery、MCP tool inventory、root policy、项目可写性、provider executable 和路由边界。
  - 缺口：已实现 Node(doctor.ts:123)、bundle(:166)、schema(:187)、skills 即 plugin discovery(:211)、.mcp.json manifest(:242)、项目可写性含写探针(:276)、三个 provider executable(:341)、路由边界 http.token+route.billing(:385/:410)。缺：无真正的 MCP tool inventory 检查——checkMcpManifest 只验证 .mcp.json 声明了 server，不枚举工具（16 工具清单只在 tests/wiring.test.t…
- ✅ provider auth 只做无成本/最小安全 probe；无法保证零调用时标记 `skipped` 并给出人工命令，不伪造“已登录”。
- 🟡 添加 CLI `doctor` 与 MCP `research_doctor`；退出码区分 fatal 与 advisory。
  - 缺口：实现齐全：src/cli.ts:105-113 doctor 子命令（overall=fail 时 exitCode=1，warn 保持 0，即 fatal/advisory 区分），src/server.ts:519-547 注册 research_doctor（readOnlyHint:true）。MCP 侧有测试证据（tests/wiring.test.ts:53-77 经 InMemoryTransport 调用并校验报告结构与 auth=skipped）；但 CLI doctor 命令及其退出码区分没有任何直接测试，全仓库无测试执行该命令路径
- 🟡 测试缺 CLI、失效 root、占位 HTTP token、错误模型名、包月/API 冲突和秘密脱敏。
  - 缺口：tests/doctor.test.ts 已覆盖：缺 CLI（kimi 缺失→warn+remediation）、失效 root（不可写 0o500、指向普通文件、manifest 损坏均→fail）、占位 HTTP token（'replace-me-token'→fail 且序列化不含 token）、包月/API 冲突（XAI_API_KEY/DEEPSEEK_API_KEY→route.billing warn，值不回显）、秘密脱敏（多处 not.toContain 断言）。缺：'错误模型名'完全没有覆盖——doctor.ts 与 doctor.test.ts 中均无任何模型名检查或测试…
  - 审计备注：验证方式：通读 src/doctor.ts(469行)/tests/doctor.test.ts(446行)，核对 cli.ts、server.ts 接线与 schemas/doctor-report.schema.json（存在且与 zod 定义一致），并实跑 npx vitest run tests/doctor.test.ts tests/wiring.test.ts，25/25 通过。整体评价：核心 doctor 功能扎实且安全边界（零调用、脱敏）做得比计划更保守，但三处与计划字面要求有差距——MCP tool inventory 与 root policy 无专属检查项、CLI 退出码无测试、'错误模型名'场景无覆盖。protocol.ts 未见 doctor 相关改动（计划列为 Modify），实际实现未需要改它，不算缺陷。

### Task 1.3: 提供 preset、plan builder 与可复现 workflow lock  — done 3 / partial 2 / missing 0

- ✅ 固定七个 preset：`quick-review`、`blind-triad`、`full-panel`、`producer-reviewer-revision`、`manuscript-strict`、`figure-audit`、`code-science-audit`。
- 🟡 plan builder 只询问会改变协议的字段：数据包、盲/开放、角色、模型路由、预算、deadline、科学 profiles。
  - 缺口：planner.ts:74-104 OverridesSchema 严格限定输入为 packet_id/mode/adapters/models/limits/briefs，未知字段与未知节点被拒（planner.test.ts:242,254 覆盖）。但计划点名的"科学 profiles"在 src/ 中完全不存在（grep profile 无命中，该概念在 roadmap 后续任务才出现）；预算/deadline 只以 limits（max_wall_time_ms、max_prompt/output_chars 等）近似体现；角色由 preset 固定不可询问
- 🟡 输出不可变 `workflow.lock.json`，记录 preset 版本、skill IDs/versions、provider/model、能力缺口和生成器版本。
  - 缺口：planner.ts:48-62 WorkflowLockSchema 记录 preset_version、generator_version(planner/0.2.0)、provider_routes(adapter/model/route)、capability_gaps、plan_hash、skill_ids；writeLock 经 writeImmutableFile 的 wx 标志写一次即拒绝覆盖（planner.test.ts:341 通过），schemas/workflow-lock.schema.json 与 Zod 一致性有测试（:355）。缺：计划点名的 skill "v…
- ✅ MCP 提供纯预览 `research_build_plan`；只有显式 `--write`/工具参数才落盘，且不覆盖已有非空计划。
- ✅ 属性测试 DAG 无环、盲审 barrier 完整、adjudicator 依赖全体盲审报告、同一 actor 不自裁决。
  - 审计备注：实测命令：npx vitest run tests/planner.test.ts tests/wiring.test.ts → 2 文件 30 测试全部通过。整体判断：Task 1.3 主体已实现且接线完整（CLI build-plan + MCP research_build_plan 均在），两处 partial 都是计划点名但磁盘上不存在的子项——\"科学 profiles\"作为 builder 输入完全缺失（roadmap 中该概念属后续任务，可能是有意推迟），lock 中 skill 只有 ID 没有 version（仓库里 skill 本身无版本号可锁）。另注：\"不覆盖已有非空计划\"实现为不覆盖任何已存在文件（wx 语义），比计划表述更保守。

### Task 1.4: 增加 dry-run 成本、时间和风险预览  — done 4 / partial 1 / missing 0

- 🟡 计算节点数、并行宽度、最坏重试数、输入字符/近似 token、输出上限、最长墙钟和每 provider 并发。
  - 缺口：src/forecast.ts:241-256 输出 node_count、max_parallel_width、worst_case_invocations、prompt_char_upper_bound、output_char_upper_bound、wall_time_upper_bound_ms，均有测试（forecast.test.ts 29/29 通过，含 20 个随机计划不变量）。缺："每 provider 并发"没有实现——ProviderForecastSchema（forecast.ts:28-33）只有 nodes/worst_case_invocations/route…
- ✅ route 必须显示 `subscription_cli | metered_api | fake | unknown`；任何 metered/unknown 路由默认给出阻止性警告。
- ✅ 不声称知道供应商真实剩余额度；只显示用户配置预算和理论上界。
- ✅ `dry-run` 绝不启动 provider，不创建 workflow events，只允许生成可审计 forecast 文件。
- ✅ 使用 fake 和故障计划验证估算守恒、重试上界和零付费调用。
  - 审计备注：整体判定：Task 1.4 基本完成，5 条中 4 条 done、1 条 partial。计划点名的五个文件全部存在且接线（src/forecast.ts 新建、cli.ts dry-run 命令、server.ts research_dry_run 工具、tests/forecast.test.ts，schemas/forecast.schema.json 由 generate-schemas.ts:108-112 从 ForecastSchema 生成）。protocol.ts 本身没有 forecast 相关改动，但 forecast.ts 从它导入 RoundtablePlanSchema，功能上不构成缺口。public-schema.test.ts 未覆盖 forecast.schema.json 的同步性（只测 event 和 plan schema），属次要观察。唯一实质缺口…

### Task 1.5: 减少接受与注意力管理摩擦  — done 2 / partial 3 / missing 0

- 🟡 `prepare-acceptance` 只把最新 passing verification ID/hash 写入指定 approval 的 `accepts`；`status`、`authority`、`note` 保持人工填写。
  - 缺口：核心已实现且有测试：src/acceptance-helper.ts:129 prepareAcceptance 只改 accepts 的 verification_event_id/hash（writeAcceptsValue 原地改标量保留格式）；tests/acceptance-helper.test.ts 'fills only the accepts block...keeps every other byte' 验证 status/authority/note 逐字节不动，测试通过。缺：计划要求 Modify cli.ts/server.ts，但两文件 grep 无 prepare…
- ✅ 如果 verification 已过期、存在后续协议事件或有未决 blocker，helper 必须拒绝。
- 🟡 attention watcher 以队列内容哈希去重，本地通知失败只记录非权威诊断，不改变状态。
  - 缺口：去重与非权威失败处理已实现并有测试：src/attention.ts:44 buildAttentionDigest 产出 digest_hash，:86 shouldNotify 按哈希去重，:126 notifyLocal 永不抛错、失败只返回 notifier-failed 并记入进程内日志（defaultNotifier :98）；tests/attention.test.ts 验证不写任何账本事件。缺：没有真正的常驻 watcher 循环——attention.ts 只导出纯函数，cli.ts/server.ts 无任何 attention 引用，watch 进程接线未做。
- 🟡 提供 `--no-notify`、安静时段和“醒来后第一项”摘要；默认不接第三方账户。
  - 缺口：库层能力齐全且有测试：src/attention.ts NotifyOptions.enabled=false（:133，测试 'does nothing when disabled'）、quietHours 含跨午夜/归一化/空窗口用例、wakeSummary 醒来摘要（tests/attention.test.ts 均通过）；默认 notifier 为本地内存记录器，不连任何第三方账户。缺：字面上的 `--no-notify` CLI 旗标不存在——cli.ts grep 无 no-notify/notify/attention 引用，CLI 接线未做。
- ✅ 测试 helper 不会把 provisional review 提升成 acceptance，也不会替用户写具名权威。
  - 审计备注：验证方式：读取 /Users/waste/research-steward-worktrees/v02-v1/plugins/research-steward/src/acceptance-helper.ts、src/attention.ts 及两个测试文件全文，并在仓库内实际运行 `npx vitest run tests/acceptance-helper.test.ts tests/attention.test.ts`，2 个文件 28 个测试全部通过（attention 14、acceptance-helper 14）。接线状况：grep 确认 cli.ts、server.ts、store.ts 均无 acceptance-helper/attention/prepare-acceptance/no-notify 引用（store.ts:1970 的 attention_requi…

### Task 2.1: 建立 typed provider failure 与保守 retry policy  — done 2 / partial 3 / missing 0

- 🟡 分类 `quota | auth | model_not_found | timeout | transport | invalid_output | cancelled | unknown`，保留原始输出哈希而非秘密文本。
  - 缺口：src/provider-failure.ts:8-17 八类枚举，classifyProviderFailureDetailed 结构化码优先+多语 pattern；FailureEvidenceSchema 只收 sha256 哈希并拒绝原文字段；tests/provider-failure.test.ts 验证真实账本事件只含 stdout_hash/stderr_hash、不含敏感原文。缺：providers.ts/workflow.ts 从未调用 classifyProviderFailure，真实失败事件里没有 failure_class
- 🟡 默认：quota/auth/model_not_found/cancelled/timeout 不重试；transport 可有限重试；invalid_output 仅在计划显式允许时做一次短格式修复。
  - 缺口：src/retry-policy.ts decideRetry 精确实现 quota/auth/model_not_found/cancelled/timeout/unknown 不重试、transport 限 0-2 次、invalid_output 仅显式开启时修复一次，tests/retry-policy.test.ts 全覆盖并通过。缺：workflow.ts:485-520 自带 for 循环对任何失败类别（含 quota/auth）一律 catch-continue 重试到 retry_limit，未接 decideRetry；protocol.ts 计划 limits 无 rep…
- 🟡 每次 attempt 使用稳定 invocation ID，retry reason 与 backoff 成为事件证据。
  - 缺口：RetryDecision 已含 reason/backoff_ms（src/retry-policy.ts:76-80），FailureEvidenceSchema 含 attempt/classified_by 可入账本。缺：全 src 无任何稳定 invocation ID（仅 workflow.ts:173 的 run ID，src/invocations.ts 属 Task 2.2 尚不存在），retry reason 与 backoff 从未写入任何事件
- ✅ 用 provider fixtures 测试错误分类，不依赖供应商英语文案的单一字符串。
- ✅ 测试总调用次数永不超过 dry-run 预报上界。
  - 审计备注：仓库路径 /Users/waste/research-steward-worktrees/v02-v1/plugins/research-steward。npx vitest run tests/retry-policy.test.ts tests/provider-failure.test.ts 全部通过（39 tests）。整体判断：Task 2.1 的两个新模块（src/provider-failure.ts、src/retry-policy.ts）及其测试已完成且质量较高，但计划点名的 Modify providers.ts / Modify workflow.ts 完全未动——grep 确认这两个文件（以及 protocol.ts）没有任何对 retry-policy / provider-failure 的 import 或调用；workflow.ts 现有重试循环对失败类别不…

### Task 2.2: 增加取消、幂等 invocation 和崩溃恢复  — ⬜ 未开始（5 格，工作量：中）

- ⬜ 协议新增 `invocation_started`、`invocation_finished`、`invocation_unknown`、`run_cancel_requested`、`run_cancelled`。
- ⬜ `runProcess()` 接收 `AbortSignal`，在 macOS/Linux 终止进程组并等待退出；超时升级到 SIGKILL 的行为继续有界。
- ⬜ 增加 MCP `research_cancel_run` 和 CLI `cancel`；取消请求不可删除已完成事件。
- ⬜ 启动后崩溃但没有完成事件的调用标为 `unknown`，默认不自动重放付费调用，需要显式 resume policy。
- ⬜ 故障注入覆盖断电窗口、HTTP client disconnect、重复 cancel、重复 resume 和迟到 provider 输出。

### Task 2.3: 跨进程 provider 并发、预算和公平性  — ⬜ 未开始（5 格，工作量：中）

- ⬜ 使用 generation-fenced directory lease 扩展跨进程 permit，不复用项目事件锁。
- ⬜ runtime 目录可配置并按用户隔离；不存凭证，只存 lease、provider、启动时间和匿名 invocation ID。
- ⬜ 支持每 provider 并发、每 run 调用上限和用户定义时间窗预算；未知真实额度时不猜测。
- ⬜ 公平队列避免一个大 run 饿死短任务，取消的 waiter 必须立即释放位置。
- ⬜ 多进程测试验证 permit 上限、过期 lease 恢复、ABA 防护和无泄漏释放。

### Task 2.4: 账本索引、离线维护和恢复演练  — done 2 / partial 2 / missing 1

- ✅ 索引是可删可重建的缓存，包含 checkpoint sequence/head hash/offset；权威事件仍逐个验证到 checkpoint。
- 🟡 `maintenance inspect` 只读报告 tombstone、断裂 head、陈旧索引和备份状态。
  - 缺口：库函数 inspectMaintenance (src/maintenance.ts:115-176) 覆盖 tombstone 计数/最老年龄、head_consistent、索引 present+stale、backups 存在性，只读；tests/maintenance.test.ts:82-156 通过。缺：cli.ts 无 maintenance 子命令（case 仅 init/freeze/append/events/status/render/roundtable/verify/provisional-review/accept/resolve-block/package），se…
- 🟡 `maintenance plan` 生成动作清单；`apply` 要求显式离线证明和精确目标，不静默重写事件。
  - 缺口：planMaintenance 纯函数 (src/maintenance.ts:178-198)；applyMaintenance 要求 offline_confirmed:true 否则抛 MAINTENANCE_REQUIRES_OFFLINE(:253)，target 数与重扫不符抛 MAINTENANCE_PLAN_STALE(:266)，deleteTombstones 二次校验且永不进入 events/frozen/packages(:206-230)；tests/maintenance.test.ts:158-259,320-352 通过。缺：同上，CLI/MCP 接线未做。
- ✅ 增加隔离目录 restore rehearsal：从备份恢复后运行完整 verify 并对比 active packet identities。
- ❌ 用 10k/100k fake events 做性能回归，同时注入尾部删除、中间篡改和陈旧 checkpoint。
  - 缺口：全仓 grep 10_000/100_000/10k/100k 无相关命中。仅有 tests/ledger-index.test.ts:230-241 的 1000 事件 performance smoke，且注释明言只打印耗时不断言阈值；尾部删除/中间篡改/陈旧 checkpoint 注入只以 12 事件小规模正确性测试存在，无计划要求的 10k/100k 规模组合注入回归。
  - 审计备注：整体判定：核心库实现质量较高且测试扎实（34 用例全过），但 Task 2.4 计划点名的 store.ts/cli.ts 修改完全没做——src 内除两个新模块自身外无任何文件 import ledger-index 或 maintenance，readEventsWithIndex 没有生产调用方，索引缓存目前只被测试消费。另需注意：ledger-index.test.ts 有 4 个用例在 vitest 默认 5s 超时下会超时（本机慢 I/O），需加长 testTimeout 才通过，这对未来补 10k/100k 回归的可行性是个信号。参考线索与磁盘状态一致。

### Task 2.5: 本地可观测性与评估基础  — done 3 / partial 2 / missing 0

- ✅ 定义 OpenTelemetry-compatible span 字段：run/node/actor/provider/model、queue/duration、attempt、status、hash和成本类别。
- ✅ 默认 trace 不含原始 prompt/output；项目 opt-in 后也只写本地、权限受限目录。
- ✅ 固定 benchmark case、预期 finding、可接受决定和已知不可判断项。
- 🟡 提供 JSON/OTLP file export；Langfuse/MLflow 导出必须单独启用并经过脱敏器。
  - 缺口：OTLP file export 已实现且有测试：exportOTLPFile（src/telemetry.ts:227-263，writeImmutableFile 防覆盖）+ JSONL 落盘，测试 'exports OTLP/JSON resourceSpans and refuses to overwrite' 通过；但 grep -rni 'langfuse|mlflow' src tests evals 零命中——Langfuse/MLflow 导出器（含单独启用开关和脱敏器管道）完全未实现
- 🟡 测试 secrets、绝对私密路径和冻结正文不会进入默认 trace。
  - 缺口：secrets（Bearer/sk-/ghp/github_pat/xox/AKIA/ASIA/64+hex）和绝对私密路径（/Users/*、/home/*）的 redact 测试齐全并通过（tests/telemetry.test.ts:47-68、110-127）；raw prompt/output 被 allowlist 丢弃也有测试；但 grep 'frozen|冻结' 在 tests/telemetry.test.ts 零命中——没有测试专门植入冻结 packet 正文内容并断言其不出现在默认 trace，该覆盖项只被 raw-content 丢弃测试间接覆盖
  - 审计备注：测试证据为实际运行：npx vitest run tests/telemetry.test.ts 全部 23 个用例通过。另一个值得上游知道的事实：TelemetryRecorder 目前只存在于 src/telemetry.ts，未接入 workflow/cli/server（模块头注释明确把 wiring 推迟到后续任务）；Task 2.5 的文件清单不含 wiring 文件，所以未据此降级，但 span 目前没有任何生产调用方。evals/README.md 已写明 gold 标签来源要求和 CI 零费用约束，与 checkbox 3 一致。

### Task 2.6: diff-based re-review 与 finding/remediation 生命周期  — ⬜ 未开始（5 格，工作量：中）

- ⬜ diff packet 绑定 base/target packet hashes、变更文件、上下文窗口和完整 target identity。
- ⬜ finding 状态区分 `open | fixed | accepted_risk | obsolete | deferred`；只有具名 adjudication 能改变 authoritative disposition。
- ⬜ `fixed` 必须引用 remediation evidence 和新 packet；不能由作者一句“已修”关闭。
- ⬜ 复审模型可以只看 diff，但 deterministic verify 始终检查完整 target packet。
- ⬜ 测试重命名、二进制文件、行号漂移、旧 finding 重新出现和 accepted-risk 过期。

### Task 3.1: 冻结 Research Contract 与分析边界  — ⬜ 未开始（5 格，工作量：中）

- ⬜ 合同字段包括 question、claim scope、estimand、population、intervention/exposure、comparator、outcome、time、unit、data cut、assumptions 和 exploratory/confirmatory。
- ⬜ 合同版本作为 freeze/roundtable 依赖；合同变化要求新 packet，不改写旧结论上下文。
- ⬜ 不适用字段必须显式 `not_applicable` + reason，不能靠空字符串混过去。
- ⬜ 提供通用、实验、观察性、模型/仿真、文献综述 profile。
- ⬜ 测试范围漂移、post-hoc confirmatory 伪装和不一致 outcome/unit。

### Task 3.2: 把证据定位器升级为结构化、可验证对象  — ⬜ 未开始（5 格，工作量：中）

- ⬜ 使用 discriminated union：`file_range`、`artifact`、`command_result`、`url`、`doi`、`dataset_record`。
- ⬜ 文件证据记录相对路径、line/page/sheet/cell locator、内容或文件 hash；URL 记录 retrieval timestamp 和 content hash。
- ⬜ 命令证据记录 argv 数组、cwd identity、exit code、stdout/stderr hash 和工具版本，永不保存 shell 插值字符串。
- ⬜ 提供 protocol v1 free-text locator 的只读兼容和显式升级工具；不猜测无法解析的旧 locator。
- ⬜ 属性测试 canonicalization、路径逃逸、URL fragment、表格单元格和 PDF 页码边界。

### Task 3.3: 建立 focused scientific audit skills  — ⬜ 未开始（5 格，工作量：轻）

- ⬜ 每个 skill 保持短路由、明确触发范围、所需输入、禁止推断项、输出 schema、验证脚本和来源 references。
- ⬜ statistics profile 覆盖效应量/CI、功效、假设、缺失、多重比较、数据泄漏和 sensitivity analysis。
- ⬜ bias/validity profile 区分混杂、选择、测量、信息泄漏、外部效度和因果主张边界。
- ⬜ reporting profile 以可选择清单接入 CONSORT/STROBE/PRISMA 等，不把清单通过等同于科学正确。
- ⬜ 所有 skills 通过 frontmatter、链接、脚本、许可证、供应商引导和 fake fixture smoke。

### Task 3.4: 显式、受限的确定性命令执行器  — ⬜ 未开始（5 格，工作量：中）

- ⬜ 新工具 `research_run_check` 只接收结构化 executable + argv，不接收 shell 字符串。
- ⬜ 项目 policy 明确 executable allowlist、root、network、CPU/wall/output、环境变量和并发上限。
- ⬜ 默认无网络、最小环境、项目根内 cwd；需要网络或外部路径时必须单独批准。
- ⬜ 结果以 `command_result` evidence 提交；失败不被模型文本覆盖。
- ⬜ 测试命令替换、路径逃逸、symlink、进程树、输出炸弹、超时和不允许的环境变量。

### Task 3.5: Claim–Evidence Matrix 与全链路 lineage  — ⬜ 未开始（5 格，工作量：中）

- ⬜ claim 类型区分 descriptive/associational/causal/predictive/mechanistic/methodological。
- ⬜ 记录支持、削弱、矛盾和空证据；证据强度与不确定性不能被多数模型自动提升。
- ⬜ lineage 节点覆盖 source→transform→analysis→table/figure→manuscript claim，并保存版本/hash。
- ⬜ 生成 revision trajectory 和 coverage map，显示未覆盖的主张、变量、反证与视角。
- ⬜ 测试一份来源被替换后下游 figure/claim 自动标为 stale，而不是自动判错。

### Task 3.6: 引用真实性、支持度和撤稿检查  — ⬜ 未开始（5 格，工作量：重）

- ⬜ 将存在性、元数据一致性、主张支持度和撤稿/更正状态分开，任何一项通过不替代其他项。
- ⬜ 网络结果记录查询、时间、响应 hash 和来源；缓存过期策略写入证据。
- ⬜ DOI/标题/作者模糊匹配只产生候选，不自动重写参考文献。
- ⬜ PaperQA2 可作为可选本地 connector 候选；必须先用固定 corpus 验证 locator 和支持度误差。
- ⬜ 离线时明确标记 `not_checked`，不把网络失败解释为“没有撤稿”。

### Task 3.7: 建立科研与评审校准 benchmark  — ⬜ 未开始（5 格，工作量：重）

- ⬜ 至少覆盖引用错配、数值闭合、图表来源、统计假设、因果越界、代码/结果不一致和刻意不可判断。
- ⬜ gold 标签由人或独立证据裁决产生，并记录 adjudicator 与依据；模型输出不能自封 gold。
- ⬜ 报告 precision/recall/FNR/FPR、遗漏类型、成本、延迟和不稳定性；不只给总分。
- ⬜ prompt、skill、model、provider、route、temperature/模式和 frozen corpus identity 全部版本化。
- ⬜ CI 只跑零费用 deterministic/fake 子集；真实模型 eval 需要用户另行批准和预算。

### Task 3.8: 可选复现工具适配器  — ⬜ 未开始（5 格，工作量：中）

- ⬜ 每个适配器只探测版本与 artifact identity，除非通过 Task 3.4 policy 明确授权才执行命令。
- ⬜ DVC 记录 stage/params/data hashes；marimo 记录纯 Python notebook identity；Quarto 记录 source/output；GE 记录 expectation suite/result。
- ⬜ 项目不使用这些工具时完全零依赖、零警告噪音。
- ⬜ 适配器失败只影响对应可选 gate，不改变核心协议可用性。
- ⬜ 用最小 fixtures 验证来源改变会使输出 stale，且不会误把缺工具当科学失败。

### Task 4.1: 冻结 coordinator–worker 协议  — ⬜ 未开始（5 格，工作量：重）

- ⬜ 定义 worker capability、job lease、heartbeat、attempt、result receipt、cancel 和 stale-result 行为。
- ⬜ job 使用 content-addressed input 与 idempotency key；worker 不获得任意服务器路径。
- ⬜ confidentiality level 与 capability 匹配失败时拒绝分配，不做自动降级。
- ⬜ 同一 job 的多个 attempt 可存在，但只有协议允许的一个结果进入下游；迟到结果保留为证据。
- ⬜ 用模型检验/状态机测试覆盖重试、分区、重复 claim、lease steal 和 coordinator 重启。

### Task 4.2: VPS coordinator 与持久任务队列  — ⬜ 未开始（5 格，工作量：重）

- ⬜ coordinator 只保存任务元数据、同步后的授权 packet、租约和结果；不保存 provider OAuth/API 凭证。
- ⬜ worker 使用 scoped、可撤销、短期 token；每个 token 绑定项目/能力/操作。
- ⬜ 队列采用 pull 模式，避免 VPS 主动进入本地设备；所有提交校验 input/output hash。
- ⬜ systemd hardening、loopback/private bind、日志脱敏、备份和重启恢复作为发布门槛。
- ⬜ 先在隔离 fake worker 上完成 soak test，用户另行批准后才触及真实 VPS。

### Task 4.3: Mac/Mac Mini 本地模型 worker  — ⬜ 未开始（5 格，工作量：重）

- ⬜ worker 启动时声明 provider CLI/model/skill/CPU/RAM/保密能力，不上传本地账号资料。
- ⬜ Qwen/GLM/Grok/Kimi 路由沿用 sealed cwd、无 shell、输出上限和不静默 API fallback。
- ⬜ 支持 pause/drain/cancel、安静时段、资源上限和网络断开后的安全恢复。
- ⬜ launchd 文件只引用外部 secret/env 位置，不包含真实 token。
- ⬜ fake 与本地无成本 provider probe 通过后，真实模型测试仍需逐次预算授权。

### Task 4.4: HPC deterministic worker  — ⬜ 未开始（5 格，工作量：重）

- ⬜ 只接受 Task 3.4 已批准的确定性任务，不在 HPC 上运行持有个人订阅凭证的模型 CLI。
- ⬜ 生成脚本固定 `computeshort`、≤1h、array 优先、≤480 cores、QoS normal/ood。
- ⬜ 作业 ID、脚本 hash、input manifest、环境/module、exit 和 output hashes 写回 attempt。
- ⬜ 提交、取消和结果拉取分开授权；密码或票据只用于当前会话，不落盘。
- ⬜ 静态 policy 测试先于任何真实 `sbatch`；真实 HPC smoke 需用户单独批准。

### Task 4.5: 分层远程访问与通知  — ⬜ 未开始（5 格，工作量：重）

- ⬜ Tailscale 保持第一恢复路径；Cloudflare 文档站与写 MCP 使用不同 hostname/policy。
- ⬜ Access JWT 必须在 origin 验证；Cloudflare identity 只映射操作身份，不自动授予 scientific acceptance。
- ⬜ 通知 adapter 只消费 hash-deduplicated attention events，不读取完整冻结正文。
- ⬜ 第三方 Todoist/邮件/Slack 连接均 opt-in、最小 scope、可禁用，失败不重试到骚扰用户。
- ⬜ 外部 DNS/Tunnel/Access 变更仍需单独、即时授权和回滚快照。

### Task 5.1: 建立非聊天式 Research Control Plane 壳层  — ⬜ 未开始（5 格，工作量：重）

- ⬜ 三栏信息架构：左侧项目/阶段 rail，中间 DAG/证据，右侧 attention/人工决定。
- ⬜ UI 只通过 read model 读取物化视图；任何写动作调用现有治理 API，不直接编辑事件文件。
- ⬜ 首屏在 3 秒内回答状态、阻塞、下一决定、最近验证 identity；长日志按需展开。
- ⬜ 支持浅/深色、响应式、键盘导航、reduce motion、WCAG 2.2 AA 和统一状态色语义。
- ⬜ 评估 `@xyflow/react` 后锁定版本和许可证；不依赖已归档 Flowise。

### Task 5.2: 科研 DAG、证据矩阵与 revision trajectory 视图  — ⬜ 未开始（5 格，工作量：重）

- ⬜ DAG 节点显示 actor/model/route/status/uncertainty/dependencies，不显示隐藏 chain-of-thought。
- ⬜ Evidence Matrix 可从 claim 跳到 locator、packet、finding、decision 和 remediation。
- ⬜ coverage map 标出未覆盖视角、变量、反证、引用和 scientific profiles。
- ⬜ cost/time 卡区分估算与实际、包月 route 与 metered route，未知值明确显示 unknown。
- ⬜ 用大项目、长标题、中文/英文、色盲和窄屏视觉回归测试。

### Task 5.3: 人工审批与自包含已填写 HTML  — ⬜ 未开始（5 格，工作量：重）

- ⬜ 主按钮中文为 `导出已填写版 HTML`，英文为 `Export filled HTML`。
- ⬜ 导出文件内嵌当前选择、备注、verification identity 和 schema version，安全转义 `</script>`、HTML 和 Unicode 边界。
- ⬜ 保留 blank/source HTML，导出新文件名包含 `已填写版`/`filled`；导出文件仍可编辑并再次导出。
- ⬜ 全新浏览器、无 localStorage、离线环境打开后，所有输入可见且无重复 UI/运行时错误。
- ⬜ 导出的表单是待签意见，不自动生成 acceptance event；回传后仍需具名权威提交。

### Task 5.4: 交付状态机与 byte receipt  — ⬜ 未开始（5 格，工作量：中）

- ⬜ 完整实现 `candidate_declared → delivery_authorized → delivery_recorded → delivery_verified`。
- ⬜ 上传 connector 与本地 package 分离；任何外部写入需要精确 destination 和即时授权。
- ⬜ receipt 记录远端 object/version/size/hash/time；无法验证 byte identity 时不得进入 verified。
- ⬜ 重复上传用 idempotency key，destination mismatch/overwrite/version drift 均 fail closed。
- ⬜ 提供 fake destination 和本地 filesystem fixture；真实云端 connector 逐个另行授权。

### Task 5.5: Actor/worker 身份与事件签名  — ⬜ 未开始（5 格，工作量：中）

- ⬜ 使用可选 Ed25519 detached signature 绑定 canonical event bytes、project ID、actor ID 和 key ID。
- ⬜ 私钥只在本地 OS secret store/受限文件中；仓库与 VPS 只保存公钥和撤销状态。
- ⬜ key rotation/revocation 不删除历史，旧事件按签署时有效性验证。
- ⬜ 签名证明“谁的密钥提交了事件”，不证明事件科学正确，也不自动授予 acceptance 权限。
- ⬜ 测试 replay、跨项目复制、key substitution、过期/撤销和 unsigned legacy events。

### Task 5.6: 科研 skill catalog、质量门槛和 composition recipes  — ⬜ 未开始（5 格，工作量：轻）

- ⬜ catalog 记录 skill ID、版本、阶段、领域、输入/输出、风险、依赖、客户端兼容性、来源和许可证。
- ⬜ validator 检查 frontmatter、目录/名称、registry/disk、相对链接、脚本语法、外链、许可证和 vendor steering。
- ⬜ recipes 组合文献综述、实验、图表、论文、审稿回复、代码+科学审计等 focused skills，不加载一个巨型 prompt。
- ⬜ skill selection 生成 `skill-stack.lock.json` 与选择证据，支持 diff 和缺口报告。
- ⬜ 社区贡献必须附 fixture、失败边界、来源和许可证；星标不是准入条件。

### Task 5.7: 可维护性、供应链和发布治理  — ⬜ 未开始（5 格，工作量：重）

- ⬜ 用 characterization tests 先冻结行为，再把 store 拆成 ledger/views/packets/verification/acceptance，workflow 拆成 graph/execution/barrier/adjudication。
- ⬜ server 工具注册改为小模块，保持 tool names/schema backward compatibility。
- ⬜ release 生成 SBOM、依赖审计、bundle/source identity、provenance、changelog 和 stable/beta channel。
- ⬜ Dependabot/安全告警不自动合并；每次升级跑 clean-room install、三 transport smoke、schema diff 和 secret scan。
- ⬜ 每季执行备份恢复、key rotation、worker revoke、VPS rollback 和 Cloudflare/Tailscale recovery 演练。

**合计：✅ 21 ｜ 🟡 18 ｜ ❌ 1 ｜ ⬜ 115（共 155 格）**。加权（done=1、partial=0.5）≈ 30/155 ≈ 19%。

## 4. Codex 监督 finding 处置表（RS-V1-SUP-001…020）

全文见 `docs/HANDOFF_TO_CLAUDE_2026-08-31.md` §18–27。裁决（accept/partial）为 Claude 在 ledger 中的正式 disposition；带"RED"者 Codex 有可复现失败反例。

| # | 级别 | 主题 | 裁决 | 状态（2026-09-05） |
|---|---|---|---|---|
| 001 | Important | 1.1 先冻结依赖门槛被违反（1.2–1.4 先行写入） | accept | 已缓解：079dbb0 上独立复跑 87/87+bundle identity |
| 002 | Important | 共享工作树多写入者，测试不可归因 | accept | 已改单 integrator 串行；任务证据在稳定 commit 重跑 |
| 003 | Important | 基线测试记录滞后于实现 | partial | 已在 079dbb0 建立新完整基线 |
| 004 | Important | installed smoke 未走真实 tag marketplace/新 client 发现链 | partial | 未修——backlog：tag-ref lane + 新进程发现检查 |
| 005 | Important | coverage 阈值"只升不降"未被测试锁定 | accept | 未修——需断言 >=75/77/84/75 下界 |
| 006 | Important | doctor 缺 MCP inventory/root policy/model-route 检查与错误模型名用例 | accept | 未修 |
| 007 | Important | workflow lock 缺 skill version/profile/budget 复现身份 | partial | 未修——至少冻结现有标识并声明 capability gap |
| 008 | Important | forecast 字符上界漏乘 retry attempts（低估至 1/3） | accept | 未修 |
| 009 | Important | max_parallel_width 低估（调度反例）；metered/unknown route 结构化不可达 | accept | 未修 |
| 010 | Important | research_doctor 误标 readOnlyHint（实际写 probe 文件） | accept | 未修 |
| 011 | Important | plan+lock 双文件写入可产生半提交 | accept | 未修——需成对 preflight+staged write |
| 012 | Important | 未逐条回应 findings 前继续跨阶段并行 | accept | 已回应：ledger 处置表+本文档；接线冻结遵守中 |
| 013 | **Critical** | eval fixture 含用户真实姓名+私人邮箱；虚构人工裁决出处 | accept | **已修**：历史改写 333292b + e4af416 机器强制；备份分支留档 |
| 014 | Important | prepareAcceptance 并发覆盖人类审批（RED 反例） | accept | 未修——下一优先；需 CAS/租约 + ACCEPTANCE_DOCUMENT_CHANGED |
| 015 | Important | ledger index 无 offset/加速读路径/10k-100k 回归 | accept | 未修 |
| 016 | Important | maintenance plan 仅绑定数量，same-count replacement 被误删（RED） | accept | 未修——下一优先 |
| 017 | Important | 目录形 index rebuild 裸 EISDIR（RED） | accept | 未修——下一优先；需 blocked/quarantine action |
| 018 | Important | retry policy 未接线：quota 失败仍真实调用 3 次（RED） | accept | 未修——Task 2.2 接线时首修；修复前禁真实 provider |
| 019 | Important | telemetry 跟随 symlink/保留 0644/漏 Windows home（3 RED） | accept | 未修——下一优先 |
| 020 | Important | checkbox 自查不能替代 finding intake（process gate） | accept | 已回应：处置表建立；接线批冻结维持 |

## 5. 下一步顺序（Codex 定序 + conflict-scan 裁决）

1. 修带 RED 反例的破坏/成本/隐私门：**014**（acceptance CAS/租约）、**016**（maintenance 精确目标绑定）、**017**（目录形 rebuild quarantine）、**019**（telemetry no-follow/收权/Windows 脱敏）；每项先把 Codex 的反例变成正式 RED 测试再修绿。
2. 补功能完整性：**015**（index offset/性能契约或诚实改名）、004/005/006/007/008/009/010/011。
3. 第二接线批次（1.5 prepare-acceptance + 2.4 maintenance 入 CLI/MCP，工具数 16→约 18，同步更新三个 smoke 断言）——**当前被 012/020 冻结，需上述修复+Codex scoped re-review 后解冻**。
4. Task 2.2（取消/幂等/崩溃恢复；providers.ts/workflow.ts 串行改造，018 在此接入 policyFromPlanLimits+decideRetry）→ 2.3 → 2.6 → Phase 2 故障注入 gate + PHASE-2 报告 + Codex phase review。
5. Phase 3（科研合同与证据，需用户提供三个脱敏真实样例）→ Phase 4（fake 分布式，真实 VPS/HPC 走审批）→ Phase 5（控制台/交付/发布治理）。
6. 全程：每 Task commit+里程碑+queue 行；Codex 审查往返；用户级门槛写 USER_ACTION_REQUIRED.md。

时间量级（按 2026-08-31 实测吞吐折算）：Phase 2 收尾 2–3 个并行工作窗口；Phase 3 3–5 窗+样例依赖；Phase 4 2–3 窗+审批依赖；Phase 5 4–6 窗；审查往返 1–2 窗。日历时间由 Codex 往返与用户授权节奏决定。
